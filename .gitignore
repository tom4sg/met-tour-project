.venv/
.env
.env.local
.env.*.local
!.env.example
__pycache__/
.ipynb_checkpoints/
.DS_Store
.streamlit/secrets.toml

# Large Met Open Access dumps (> GitHub 100MB limit) — download locally
data/raw/
data/raw/MetObjects.csv
data/raw/MetObjects.txt

# Large embedding and GMM artifacts — regenerate locally via embed_pipeline + cluster_gmm
embeddings/**/*.npy
embeddings/**/*.npz

# Frontend build artifacts and dependencies (exceed GitHub 100MB limit)
frontend/node_modules/
frontend/.next/